FROM node:18-alpine

# 設置工作目錄
WORKDIR /app

# 複製 package.json 和 package-lock.json
COPY package*.json ./

# 安裝依賴
RUN npm ci

# 複製所有源代碼
COPY . .

# 設置環境變量
ENV NODE_ENV=test

# 安裝開發依賴
RUN npm install --only=dev

# 運行測試
CMD ["npm", "run", "test:integration"] 